import {GoogleGenerativeAI} from "@google/generative-ai"; import {z} from "zod"; import type {RecipeInput} from "./types";
const schema=z.object({title:z.string(),description:z.string().nullable().optional(),servings:z.number().nullable().optional(),prepMinutes:z.number().nullable().optional(),cookMinutes:z.number().nullable().optional(),ingredients:z.array(z.object({name:z.string(),quantity:z.number().nullable().optional(),unit:z.string().nullable().optional(),notes:z.string().nullable().optional()})),instructions:z.array(z.string()),nutrition:z.object({calories:z.number().nullable().optional(),protein:z.number().nullable().optional(),carbs:z.number().nullable().optional(),fat:z.number().nullable().optional()}).nullable().optional(),cuisine:z.string().nullable().optional(),mealType:z.string().nullable().optional(),tags:z.array(z.string()).optional(),source:z.object({platform:z.string().nullable().optional(),url:z.string().nullable().optional()}).nullable().optional()});
const prompt=`Extract this into a recipe JSON object. Do not invent values; use null when unavailable. Normalize quantities and units. Return JSON only with title, description, servings, prepMinutes, cookMinutes, ingredients, instructions, nutrition {calories,protein,carbs,fat}, cuisine, mealType, tags, source {platform,url}.`;
export async function extractRecipe(text?:string,image?:{data:string;mimeType:string},sourceUrl?:string):Promise<RecipeInput>{const key=process.env.GEMINI_API_KEY;if(!key)throw new Error("Gemini is not configured. Add GEMINI_API_KEY on the server.");const model=new GoogleGenerativeAI(key).getGenerativeModel({model:process.env.GEMINI_MODEL||"gemini-3.6-flash",generationConfig:{responseMimeType:"application/json"}});const parts:({text:string}|{inlineData:{data:string;mimeType:string}})[]=[{text:`${prompt}\nSource URL: ${sourceUrl||"none"}\nContent:\n${text||"Read the supplied recipe image."}`}];if(image)parts.push({inlineData:image});const result=await model.generateContent(parts);const value=schema.parse(JSON.parse(result.response.text()));if(sourceUrl)value.source={...(value.source||{}),url:sourceUrl,platform:platform(sourceUrl)};return value}
function platform(url:string){try{const host=new URL(url).hostname;return["instagram","tiktok","youtube","reddit"].find(x=>host.includes(x))||"website"}catch{return "website"}}
